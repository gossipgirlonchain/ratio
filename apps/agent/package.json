{
  "name": "@ratio/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ratio agent: X mentions -> two-tweet like markets on Doppler -> 24h settlement.",
  "scripts": {
    "sim": "tsx src/sim.ts",
    "sim:devnet": "tsx --env-file-if-exists=../../.env src/sim-devnet.ts",
    "typecheck": "tsc --noEmit",
    "start": "tsx --env-file-if-exists=../../.env --env-file-if-exists=.env src/main.ts"
  },
  "dependencies": {
    "@ratio/chain": "*",
    "@ratio/config": "*",
    "@ratio/doppler": "*",
    "@solana-program/system": "^0.12.2",
    "@solana/kit": "^6.10.0",
    "@supabase/supabase-js": "^2.112.3",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.23.1",
    "typescript": "5.5.3"
  }
}
