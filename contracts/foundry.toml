[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc = "0.8.26"
optimizer = true
optimizer_runs = 200
# require(cond, CustomError()) needs via-ir on 0.8.26 — same as upstream Doppler
via_ir = true

[profile.deep.invariant]
runs = 512
depth = 64
